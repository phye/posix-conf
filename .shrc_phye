alias ls="ls --color"
alias ll="ls -al"
alias tmuxn="tmux -2 new-session -s"
alias tmuxa="tmux -2 attach-session -t"
alias grep="grep -I"
alias ga="git add"
alias gc="git clone"
alias gco="git commit"
alias gd="git diff"
alias gl="git log"
alias gll="git log -p -1"
alias gs="git status"

export PATH="$HOME/bin/phye:$HOME/bin/sanity:$HOME/bin:$PATH"
export EDITOR='vim'
